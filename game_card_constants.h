#pragma once

#include "datatypes.h"
#include <cstdint>

namespace Emulator::GameCard

{

// Max src code size in bytes. 64 MB.
constexpr U32 kSize = 64 * 1024 * 1024;

} // namespace Emulator::GameCard